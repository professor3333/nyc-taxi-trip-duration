# nyc-taxi-trip-duration

Trip-duration estimation service on NYC TLC yellow-taxi data: given a pickup
zone, a dropoff zone and a departure time, return an estimated travel time in
minutes and the model version that produced it.

Work in progress. See `PROGRESS.md` for the build plan.
