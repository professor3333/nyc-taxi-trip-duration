# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets and local config
.env

# Data and models are DVC-tracked. `dvc add` writes a .gitignore next to each
# tracked file, so only .dvc pointers, READMEs and champion.json reach git.
*.part
models/*
!models/champion.json
!models/.gitignore

# Local MLflow and SQLite state
mlruns/
mlartifacts/
*.db

# Notebook noise
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/
