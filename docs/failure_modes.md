# Failure catalogue

For each failure the system is designed to survive: behaviour, the log line, the test, the runbook entry.

| failure | observed behaviour | log line | test | runbook |
|---|---|---|---|---|
| malformed input (missing field, wrong type, zone 999/264/265/0, bad or out-of-window time, extra field, empty/non-JSON body) | 422 `{request_id, errors:[{field,message}]}` | WARNING `validation_error` with field list | `test_api.py::test_validation_errors_are_422_with_fields`, `test_non_json_and_empty_bodies`, `test_fuzz_never_500`; CI container smoke `deploy_check --malformed` | — |
| unknown zone reaching the predictor | cannot happen: schema bounds 1–263; `build_features` raises on unknown ids as a second guard | ERROR `internal_error` (would be a bug) | `test_features.py::test_unknown_zone_raises` | — |
| model file missing | fallback table serves; `/health` degraded with `load_error`; `/ready` 503 | ERROR `model_load_failed` + traceback at startup | `test_missing_model_degrades_to_fallback` | service degraded |
| corrupt model | same as missing | same | `test_corrupt_model_degrades_and_ready_503` | service degraded |
| feature-list mismatch (model trained with other features) | model refused; fallback serves; degraded | ERROR `model_load_failed` "feature list mismatch" | `test_feature_list_mismatch_refuses_model` | service degraded |
| **feature-list mismatch, observed live 2026-09-22** | `/health` → `{"status":"degraded","model_version":"fallback-v1","model_kind":"fallback","load_error":"ValueError: feature list mismatch: model has ['a','b'], code has [...12 features]"}`; `/ready` 503; `/predict` still 200 from the lookup table | as above, in CloudWatch | `test_promote_never_writes_repo_champion_files` (the regression that caused it) | service degraded |
| model and fallback both missing | **controlled 503**: `/health/live` 200, `/health/ready` 503, `/predict` 503 `{"error":"unavailable"}` with `retry-after: 30`; process stays up so `/version` and the logs remain available | ERROR `model_load_failed` and `fallback_load_failed`, then `unavailable` per request | `test_both_missing_serves_controlled_503` | deploy fails at `deploy_check`; roll back |
| **model loads but fails at prediction time** (raises, NaN/inf, wrong shape, non-numeric) | that request answered by the baseline (`model_kind: "fallback"`, `model_version: fallback-vN`); `/health` degraded with `predict_error`/`predict_failures` until the model answers again; `/health/ready` 503 meanwhile | ERROR `model_predict_failed` + traceback; request line `model_kind=fallback` (FallbackCount alarm) | `test_serving_failures.py::test_model_prediction_failure_answers_from_the_baseline` (5 cases), `test_degraded_clears_when_the_model_succeeds_again` | service degraded |
| model **and** baseline fail at prediction time, or the baseline returns non-finite values | 503 `unavailable`, `retry-after: 30`; process up | ERROR `fallback_predict_failed`, then `unavailable` | `test_model_and_baseline_both_failing_is_a_controlled_503`, `test_baseline_returning_nan_is_a_503_not_a_bad_answer` | roll back |
| `champion.json` corrupt or malformed | the loaded model serves, labelled `unregistered:<sha>`; `/health` degraded with `release_error`; `deploy_check --expect-version` fails | ERROR `release_metadata_invalid` | `test_corrupt_champion_json_keeps_serving_the_model` (5 cases) | rollback |
| reference data (centroids/holidays) missing or corrupt | **unavailable**: `/predict` 503, `/health/ready` 503, `/health/live` 200 | ERROR `reference_load_failed` | `test_missing_reference_data_is_unavailable_not_a_crash`, `test_corrupt_reference_data_is_unavailable`, `test_missing_holidays_is_unavailable` | rebuild the image |
| `params.yaml` / environment unusable (bad YAML, bad date, bad limits, bad `LOG_LEVEL`) | unavailable (503), not a crash | ERROR `config_invalid` | `test_corrupt_params_is_unavailable_not_a_crash` (3 cases) | fix config, redeploy |
| request body over `api.max_body_bytes` (declared, chunked, or with a false Content-Length) | 413 `{request_id, error:"payload_too_large"}` before JSON parsing | WARNING `payload_too_large` | `test_oversized_body_is_413_before_parsing`, `test_oversized_chunked_body_is_413`, `test_lying_content_length_does_not_bypass_the_limit` | — |
| batch longer than `max_batch` | 422 on `items` before any item is validated | WARNING `validation_error` | `test_batch_length_is_checked_before_items_are_validated` | — |
| slow or CPU-heavy prediction | runs off the event loop (limiter of `predict_workers`); probes and other requests keep answering | — | `test_slow_prediction_does_not_block_the_event_loop`; measured in ADR-0012 | — |
| non-champion model baked into the image | `model_version` = `unregistered:<sha>`; `deploy_check --expect-version` fails | request lines carry the version | `test_champion_version_reported_when_md5_matches`; observed 2026-09-22 with v2's pickle vs v1's champion.json | rollback |
| unexpected exception in a handler | 500 `{request_id, error:"internal"}` | ERROR `internal_error` with traceback | fuzz finds none; handler unit-tested by construction | Logs Insights by `request_id` |
| TLC month not published | ingest exits 0, nothing written. A 403/404 counts only when the month was never ingested, is within `MAX_PUBLICATION_LAG_MONTHS` (4), and the control object (zone lookup) still answers | INFO `month 2026-08 not published yet (HTTP 403 at …; control object reachable, …)` | `test_403_means_not_published`, `test_absent_month_is_not_retried`, `test_main_exits_zero_on_403`, `test_publication_window_edge`, and the 404 equivalents | none needed |
| TLC source refusing access (403/404 that cannot be "not yet": control object fails too, month past the publication window, month we already hold, or GET refused after HEAD 200) | `SourceAccessError`: ingest exits 1, `is_published` raises so the retrain plan fails red; the accepted snapshot is untouched | ERROR `month … source access failure: …` naming the rule | `test_403_with_failing_control_is_source_access_error` (3), `test_absent_month_past_publication_window_is_source_access_error` (2), `test_absent_previously_ingested_month_is_source_access_error` (2), `test_get_refused_after_head_ok_is_source_access_error`, `test_main_exits_nonzero_on_source_access_error`, `test_is_published_raises_on_source_access_error`, `test_ingest_zones_refused_is_source_access_error` | source access failure |
| local raw file corrupt or missing while TLC's copy is unchanged | the same-ETag skip is refused; the month is downloaded again and the recorded md5 restored | WARNING `… unchanged at source but local snapshot is unusable (md5 … != recorded …)` | `test_unchanged_remote_with_corrupt_local_file_redownloads`, `test_unchanged_remote_with_missing_local_file_redownloads` | — |
| TLC transient failure (5xx/network/short read) | retried 3× with backoff, then raises (exit 1) | WARNING per retry | `test_ingest_month_retries_transient_then_succeeds`, `..._gives_up_after_retries`, `test_download_rejects_incomplete_body` | re-run |
| TLC schema drift (new/renamed column) | ingest refuses the file, exit 1 | SchemaDriftError naming the column | `test_check_schema_rejects_unknown_column`, `test_ingest_month_refuses_file_with_schema_drift` | schema drift |
| **TLC republishes a month we hold, and the new file is unacceptable** (schema drift, not Parquet, truncated) | the download stays in `data/quarantine/`; the accepted `data/raw/` file and its report are unchanged byte-for-byte; exit 1 | ERROR `… download REJECTED (…); kept at data/quarantine/… for inspection, accepted snapshot … left untouched` | `test_republish_with_schema_drift_keeps_accepted_snapshot`, `test_republish_that_is_not_parquet_keeps_accepted_snapshot`, `test_truncated_republish_keeps_accepted_snapshot`, `test_ingest_zones_rejection_keeps_previous_csv` (2) | schema drift |
| TLC republishes a month | new bytes checked in quarantine, then renamed over the old file (a new inode, so a DVC cache link is never written through); `replaced_source_md5` recorded, one `.dvc` diff | WARNING `REPUBLISHED` | `test_ingest_month_detects_republish`, `test_accepted_file_arrives_by_rename_not_rewrite` | republished month |
| promote/rollback/refresh fails before its journal (artifact store unreachable, artefact md5 ≠ registered tag, fixture generation fails) | nothing changes: aliases, `champion.json`, meta, fixture and `promotions.md` byte-identical; staging removed; re-run works | `refused: …` or the exception, exit ≠ 0 | `test_failure_before_commit_changes_nothing` (3), `test_artefact_md5_mismatch_is_refused_before_anything_changes` | interrupted promote |
| promote/rollback interrupted after its journal (mid-alias or mid-install) | every command refuses naming the operation; `--recover` finishes it, `--abort` restores the exact prior state | `refused: an interrupted promote to vN …` | `test_interrupted_operation_blocks_then_recovers` (14), `test_interrupted_operation_aborts_to_the_exact_prior_state` (7), `test_recover_is_idempotent_when_itself_interrupted` | interrupted promote |
| `champion_fixture.csv` edited or stale vs `champion.json` | promote/rollback refuse; `--refresh` repairs | `refused: … fixture_sha256 …` | `test_fixture_that_disagrees_with_the_record_is_refused` | — |
| local registry lost (laptop, Docker volumes) | deploy and rollback still work from `champion.json` + DVC; registry restored from the latest backup | — | `make registry-restore-check` (drill, run 2026-09-23 from local and S3; negative control exits 1) | registry backup and restore |
| CI training run's SQLite store discarded with the runner | its record survives as `reports/tracking/train_run.json` in the candidate (and the raw store as a 90-day run artifact); `register.py` links it | — | `test_export_run_writes_a_self_contained_record`, `test_train_run_record_is_only_evidence_for_its_own_run`, `test_retrain_preserves_its_tracking_run` | — |
| Lambda timeout / init failure / runtime crash (app never logs) | `Timeouts` / `InitFailures` / `PlatformErrors` alarms from platform lines and AWS/Lambda metrics | `Task timed out after …`, `INIT_REPORT … Status: error/timeout` | `TestMetricFilter` on real line shapes (2026-09-23); `test_platform_signals_are_alarmed` | service down or degraded, step 5 |
| slow cold start / slow network path (app latency looks fine) | `ColdStartE2E` (each new image) and `E2ELatencyP95` (daily) alarms; deploy fails if the first request is > 45 s or not provably cold | `FAIL  cold: …` in deploy_check | `test_version_proves_whether_the_process_was_fresh`, CI cold step (positive + negative), `test_parse_report_*` | service down or degraded |
| retraining stalls while the API is healthy | `freshness` issue from `monitor.yml` | `STALE  data …` | `test_freshness.py` | stale data or model |
| init exceeds Lambda's 10 s init limit (every cold start today) | Lambda redoes init inside the first invoke; request ≈ 16 s, served correctly; `InitFailures` alarm; deploy_check warns `init did not finish in the init phase` | `INIT_REPORT Init Duration: 9999.56 ms Phase: init Status: timeout` | `test_first_invocation_after_a_timed_out_init_is_cold` (the observed stream) | ADR-0008 amendment 2026-09-23 (fix candidates) |
| alert topic has no confirmed subscriber | `monitoring.sh` warns every run; `alarm_drill.py` refuses to start | `WARNING: … has not confirmed` | observed live 2026-09-23 | alert delivery |
| missing intermediate month | `prepare` refuses to skip it | ValueError naming the month | `test_month_sequence_is_contiguous_from_start` | ingest the gap |
| release image cannot load the champion / wrong model baked in / bad predictions | `deploy.yml` fails at "Smoke test the release image", before any Lambda change; production untouched | `FAIL  health status` / `FAIL  model_version` / `FAIL  predictions match champion_fixture.csv` + container log tail | `test_deploy_smoke_tests_the_release_image_before_any_lambda_change`; negative controls 2026-09-24 (ADR-0008 amendment) | fix the build or the champion; redeploy |
| candidate version fails on Lambda (alias mode) | alias `live` never moves; callers keep the previous version; run fails | `Candidate rejected before release` in the run summary | `test_alias_mode_verifies_the_candidate_before_it_takes_traffic` | investigate the published version; redeploy |
| ECR lifecycle would expire the live or rollback image (e.g. after rejected pushes) | pins + priority-1 `keep-` rule; deploy stops **before Lambda changes** if AWS's lifecycle preview would expire either | `FAIL  … WOULD EXPIRE (lifecycle preview)` / `is not pinned` / `policy: no rule selects the 'keep-' pin tags` | `test_ecr_retention.py` (failed-release scenarios, negative controls, fake ECR) | run `ecr.sh`; runbook "ECR retention" |
| rollback to a model with no recorded release (never deployed since the ledger, legacy role) or whose image expired | `deploy.yml` fails at "Plan", before any build or Lambda change; nothing is rebuilt silently | `::error::no verified release of vN is recorded …` / `image of release … is gone from ECR` | `test_rollback_restores_a_recorded_release_and_never_silently_rebuilds`, `test_find_restores_the_latest_verified_release_of_that_model` | `deploy.yml -f rebuild=true` (a new release, recorded) |
| restored release does not reproduce what it served (wrong image, different build) | deploy fails on `release_id` and on the 80-row replay at tolerance 0; restore/alias-back as for any failed deploy | `FAIL  release_id: … (expected …)`, `FAIL  predictions match served_predictions.csv` | negative control 2026-09-24 (v1 + one extra holiday: 40/80 rows off, max 23.8 min) | runbook "Rollback" |
| selected champion ≠ deployed release (deploy pending or failed) | `releases/live.json` still names what serves; monitor's version check fails; `make release-status` prints MISMATCH | `MISMATCH: the selected champion is not the deployed release` | `test_ledger_round_trip_and_live_pointer` | fix and redeploy, or roll the registry back |
| monitor cannot run a check at all (Function URL config unreadable, AWS call fails, ledger unreadable) | the step's `outcome` is `failure`; `service-health` issue opened/updated with a per-check outcome table; issue closes only when every check's outcome is `success` | `FAIL  Function URL configuration unreadable: …` | `test_monitor_workflow.py` (real step scripts under `bash -eo pipefail`; open/close over all outcome combinations); old conditions closed the issue in this case | service down or degraded |
| DVC remote unreachable at build | `fetch_champion.py` fails → `deploy.yml` fails before touching Lambda | dvc error | manual | check S3 / credentials |
| Lambda cold-start timeout | 30 s timeout → Function URL 5xx | Lambda REPORT line | `deploy_check --cold` measures | raise memory (ADR-0008) |
| ECR image missing | `update-function-code` fails; old version keeps serving | AWS error in the run | — | re-run deploy |
| DST fall-back hour in data | rows in the 3-hour window dropped in `validate` | rejection count `dst_transition_window` | `test_validate.py` DST cases | — |
| tz-aware request | converted to New York local | — | `test_tz_aware_departure_converted_to_new_york` | — |
| tz-aware request at the edge of `datetime`'s range | 422 with a field message; the conversion to New York would overflow | WARNING `validation_error` | `test_extreme_aware_timestamps_are_422_not_500` (6 cases), `test_extreme_aware_timestamp_in_batch_is_422`, fuzz over datetimes | — |

**Observed live on 2026-09-22** (account 560512681455): malformed input (all
nine cases → 422 with `request_id`, WARNING lines in CloudWatch), feature-list
mismatch → degraded fallback, Lambda cold-start timeout (at 1024 MB, before
ADR-0008's amendment), and ECR/Lambda deploy failures (OCI manifest rejected,
unreachable git revision, stale Function URL secret). Not yet observed: TLC
republishing a month, S3 unreachable at build time.
